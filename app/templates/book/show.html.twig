{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.book_view'|trans({'%title%': book.title|default('')}) }}
{% endblock %}

{% block body %}
    <h1>{{ 'title.book_view'|trans({'%title%': book.title|default('')}) }}</h1>
    {% if book is defined and book|length %}
        <dl class="dl-horizontal">
            <dt>{{ 'label.id'|trans }}</dt>
            <dd>{{ book.id }}</dd>
            <dt>{{ 'label.created_at'|trans }}</dt>
            <dd>{{ book.createdAt|format_date('long') }}</dd>
            <dt>{{ 'label.title'|trans }}</dt>
            <dd>{{  book.title }}</dd>
            <dt>{{ 'label.author'|trans }}</dt>
            <dd>{{  book.author }}</dd>
            <dt>{{ 'label.category'|trans }}</dt>
            <dd>{{  book.category.title }}</dd>
        </dl>
        <p>
            <a href="{{ url('book_index') }}" title="{{ 'action.go_back'|trans }}">
                {{ 'action.go_back'|trans }}
            </a>
        </p>
    {% else %}
        <p>
            {{ 'message.book_not_found'|trans }}
        </p>
    {% endif %}
{% endblock %}